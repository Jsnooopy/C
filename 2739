#include <stdio.h>>

int main() {
  //freopen("input.txt", "r", stdin);

  int a;
  
  for(int i = 1; i < 10; ++i){
    scanf("%d", &a);
    printf("%d * %d = %d\n", a, i, a * i);
    
  }
  
  return 0;
}
